/// <reference types="react" />
import { StyleProp, ViewStyle } from 'react-native';
declare type Alignment = 'leading' | 'center' | 'trailing';
declare type StackProps = {
    spacing?: number;
    style?: StyleProp<ViewStyle>;
    children: React.ReactElement<any> | React.ReactElement<any>[];
};
export declare type VStackProps = StackProps & {
    alignment?: Alignment;
};
export declare type HStackProps = StackProps & {
    alignment?: Alignment;
};
export declare type ZStackProps = Omit<StackProps, 'spacing'> & {
    alignment?: {
        vertical: Alignment;
        horizontal: Alignment;
    };
};
export {};
